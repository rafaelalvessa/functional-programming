CREATE TABLE prices (
    stock VARCHAR(40) NOT NULL,
    date DATE NOT NULL,
    open VARCHAR(40) DEFAULT NULL,
    high VARCHAR(40) DEFAULT NULL,
    low VARCHAR(40) DEFAULT NULL,
    close VARCHAR(40) DEFAULT NULL,
    volume BIGINT(11) DEFAULT NULL,
    adjClose VARCHAR(40) DEFAULT NULL
)
